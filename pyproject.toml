[tool.commitizen]
name = "cz_conventional_commits"
tag_format = "$version"
version = "0.2.0"
version_scheme = "pep440"
version_provider = "commitizen"
version_files = [
    "mycompanion.py:VERSION"
]
update_changelog_on_bump = true
major_version_zero = true
